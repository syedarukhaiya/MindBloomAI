# MindBloomAI Frontend

React + TypeScript + Vite interface for the MindBloomAI wellbeing companion.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` if the FastAPI server is not at the default local URL.
